{
  "name": "silverstripe-elemental-linkify",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "yarn && NODE_ENV=production webpack --mode production --bail --progress",
    "dev": "webpack --progress",
    "watch": "NODE_ENV=development webpack --watch --progress"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@silverstripe/webpack-config": "2.0.0-alpha2",
    "webpack-cli": "^5.0.1"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "modules": false
        }
      ],
      "@babel/preset-react"
    ]
  }
}
